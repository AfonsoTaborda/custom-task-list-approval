on: [pull_request]

jobs:
  create-checklist:
    runs-on: ubuntu-latest
    name: Create the Checklist
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Create TODO Checklist
        uses: ./
        id: todo
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          body: "Hello World"